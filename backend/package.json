{
  "name": "trolith-studio-backend",
  "version": "1.0.0",
  "description": "Backend API for Trolith Studio Token — reads on-chain data, serves quotes, analytics, and admin endpoints",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "db:generate": "npx prisma generate",
    "db:migrate": "npx prisma migrate dev",
    "db:push": "npx prisma db push",
    "db:studio": "npx prisma studio",
    "sync": "node src/scripts/sync-once.js"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.4",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}